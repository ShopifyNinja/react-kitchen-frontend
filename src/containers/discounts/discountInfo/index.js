import React from "react";
import DiscountInfo from "./component";

function DiscountInfoContainer({ selectedDiscount }) {
  return <DiscountInfo selectedDiscount={selectedDiscount} />;
}

export default DiscountInfoContainer;
